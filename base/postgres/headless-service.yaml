apiVersion: v1
kind: Service
metadata:
  name: cloudcart-postgresql-service
  labels:
    app.kubernetes.io/name: postgresql-service
    app.kubernetes.io/instance: cloudcart-postgresql-service
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: cloudcart
    app.kubernetes.io/managed-by: kubectl
  namespace: cloudcart
spec:
  clusterIP: None
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: cloudcart-postgresql
