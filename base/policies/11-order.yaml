apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: order-netpol
  labels:
    app.kubernetes.io/name: order-netpol
    app.kubernetes.io/instance: cloudcart-order-netpol
    app.kubernetes.io/component: network-policy
    app.kubernetes.io/part-of: cloudcart
    app.kubernetes.io/managed-by: kubectl
  namespace: cloudcart
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: order
      app.kubernetes.io/instance: cloudcart-order
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: gateway
              app.kubernetes.io/instance: cloudcart-gateway
      ports:
        - protocol: TCP
          port: 8000
  egress:
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: postgresql
              app.kubernetes.io/instance: cloudcart-postgresql
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: inventory
              app.kubernetes.io/instance: cloudcart-inventory
      ports:
        - protocol: TCP
          port: 8000
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: payment
              app.kubernetes.io/instance: cloudcart-payment
      ports:
        - protocol: TCP
          port: 8000